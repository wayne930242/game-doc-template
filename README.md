# 🎮 遊戲文件模板 (Game Documentation Template)

一個基於 [Astro](https://astro.build/) + [Starlight](https://starlight.astro.build/) 的遊戲規則文件模板，專為桌上角色扮演遊戲（TRPG）設計，但也適用於任何遊戲的規則文件。

## ✨ 特色

- 🌙 **深色模式優先** — 精心設計的深色主題，適合長時間閱讀
- 📱 **響應式設計** — 手機、平板、桌面都能完美顯示
- 🎨 **高度可自訂** — 顏色、背景、內容結構都可輕鬆修改
- 🚀 **一鍵部署** — 內建 Vercel 部署設定
- 🔍 **內建搜尋** — Starlight 提供的全站搜尋功能
- 🌐 **多語系支援** — 預設繁體中文，可擴充其他語言

## 🚀 快速開始

### 使用此模板

1. 點擊 GitHub 上的 **「Use this template」** 按鈕
2. 建立您的新 repository
3. Clone 到本地：

```bash
git clone https://github.com/your-username/your-game-docs.git
cd your-game-docs/docs
```

### 安裝與執行

```bash
# 安裝依賴（推薦使用 bun）
bun install

# 或使用 npm
npm install

# 啟動開發伺服器
bun dev
# 或
npm run dev
```

開啟 `http://localhost:4321` 預覽網站。

---

## 📁 專案結構

```
game-doc-template/
├── docs/                          # 主要文件專案
│   ├── src/
│   │   ├── assets/               # 圖片資源
│   │   │   └── hero.jpg          # 🔧 首頁主圖（替換）
│   │   ├── content/
│   │   │   ├── docs/             # 📝 Markdown 內容（主要編輯區）
│   │   │   │   ├── index.mdx     # 首頁
│   │   │   │   ├── reference/    # 快速參考
│   │   │   │   ├── rules/        # 核心規則
│   │   │   │   ├── characters/   # 角色相關
│   │   │   │   ├── items/        # 物品裝備
│   │   │   │   ├── world/        # 世界設定
│   │   │   │   └── guides/       # 主持人指南
│   │   │   └── i18n/
│   │   │       └── zh-TW.json    # 🔧 UI 翻譯（可選修改）
│   │   └── styles/
│   │       └── custom.css        # 🔧 自訂樣式
│   ├── public/
│   │   ├── bg.jpg                # 🔧 背景圖片（替換）
│   │   ├── og-image.jpg          # 🔧 社群分享圖（替換）
│   │   └── favicon.svg           # 🔧 網站圖示（替換）
│   ├── astro.config.mjs          # 🔧 網站設定
│   └── package.json
├── vercel.json                   # Vercel 部署設定
└── README.md
```

🔧 = 需要自訂的檔案

---

## 🎨 自訂指南

### 1. 基本設定 (`docs/astro.config.mjs`)

修改頂部的 `SITE_CONFIG` 物件：

```javascript
const SITE_CONFIG = {
  // 網站標題（顯示在導航列）
  title: '您的遊戲名稱',

  // 預設語言
  defaultLocale: 'zh-TW',
  localeLabel: '繁體中文',

  // SEO：設為 true 允許搜尋引擎索引
  allowIndexing: false,
};
```

### 2. 側邊欄結構

在 `astro.config.mjs` 的 `sidebar` 區塊修改：

```javascript
sidebar: [
  {
    label: '分類名稱',
    autogenerate: { directory: '資料夾名稱' },
  },
  // 或直接連結
  {
    label: '術語表',
    link: '/glossary/',
  },
],
```

### 3. 主題配色 (`docs/src/styles/custom.css`)

修改 `:root` 區塊的顏色變數：

```css
:root {
  /* 主色調 - 用於連結、按鈕、強調 */
  --sl-color-accent: hsl(210, 100%, 55%);      /* 主強調色 */
  --sl-color-accent-high: hsl(210, 100%, 80%); /* 亮版本 */
  --sl-color-accent-low: hsl(230, 60%, 18%);   /* 暗版本 */

  /* 特色配色 - 可用於不同類別的視覺區分 */
  --color-primary: #3b82f6;    /* 主要色（藍） */
  --color-secondary: #22c55e;  /* 次要色（綠） */
  --color-tertiary: #ef4444;   /* 第三色（紅） */
  --color-quaternary: #f97316; /* 第四色（橘） */
}
```

### 4. 圖片資源

| 檔案 | 位置 | 尺寸建議 | 說明 |
|------|------|----------|------|
| 背景圖 | `docs/public/bg.jpg` | 1920×1080 | 深色、低對比度為佳 |
| 社群分享圖 | `docs/public/og-image.jpg` | 1200×630 | 顯示於社群連結預覽 |
| 首頁主圖 | `docs/src/assets/hero.jpg` | 560×560 | 會裁切成圓形 |
| 網站圖示 | `docs/public/favicon.svg` | 32×32 | SVG 或 PNG |

### 5. 首頁內容 (`docs/src/content/docs/index.mdx`)

```mdx
---
title: 遊戲名稱
description: SEO 描述
template: splash
hero:
  tagline: 您的遊戲標語
  image:
    file: ../../assets/hero.jpg
  actions:
    - text: 開始閱讀
      link: /rules/
      icon: right-arrow
    - text: 快速參考
      link: /reference/
      icon: document
      variant: minimal
---
```

---

## 📝 內容撰寫

### Markdown 基本格式

```markdown
---
title: 頁面標題
description: 頁面描述（用於 SEO）
sidebar:
  order: 1  # 排序（數字越小越前面）
---

## 章節標題

內容文字...
```

### Starlight 元件

#### 提示框 (Aside)

```mdx
:::note[標題]
一般提示內容
:::

:::tip[提示]
有用的建議
:::

:::caution[注意]
需要留意的事項
:::

:::danger[警告]
危險或重要警告
:::
```

#### 卡片 (Cards)

```mdx
import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

<CardGrid>
  <Card title="卡片標題">
    卡片內容
  </Card>
  <LinkCard title="連結卡片" href="/path/" description="說明文字" />
</CardGrid>
```

#### 分頁 (Tabs)

```mdx
import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs>
  <TabItem label="選項一">內容一</TabItem>
  <TabItem label="選項二">內容二</TabItem>
</Tabs>
```

更多元件請參考 [Starlight 文件](https://starlight.astro.build/guides/components/)。

---

## 🚀 部署

### Vercel（推薦）

1. 將專案推送到 GitHub
2. 在 [Vercel](https://vercel.com) 匯入專案
3. 自動偵測設定，點擊部署

已包含 `vercel.json` 設定檔，無需額外配置。

### 其他平台

```bash
# 建置靜態檔案
cd docs
bun run build

# 輸出在 docs/dist/ 資料夾
```

可部署到任何支援靜態網站的平台（Netlify、GitHub Pages、Cloudflare Pages 等）。

---

## 📚 目錄結構建議

根據您的遊戲類型，以下是一些建議的目錄結構：

### TRPG（桌上角色扮演遊戲）

```
docs/
├── reference/     # 快速參考表
├── rules/         # 核心規則
├── characters/    # 角色創建、職業、種族
├── combat/        # 戰鬥系統
├── magic/         # 魔法/技能系統
├── items/         # 物品、裝備
├── world/         # 世界設定、歷史、地理
├── bestiary/      # 怪物圖鑑
├── adventures/    # 冒險模組
└── gm-guide/      # 主持人指南
```

### 卡牌遊戲

```
docs/
├── rules/         # 基本規則
├── cards/         # 卡牌列表
├── decks/         # 牌組構築
├── strategies/    # 策略指南
└── faq/           # 常見問題
```

### 電子遊戲攻略

```
docs/
├── basics/        # 基礎操作
├── characters/    # 角色攻略
├── quests/        # 任務攻略
├── items/         # 道具圖鑑
├── maps/          # 地圖指南
└── secrets/       # 隱藏要素
```

---

## 🛠 技術棧

- [Astro](https://astro.build/) — 靜態網站生成器
- [Starlight](https://starlight.astro.build/) — 文件主題
- [Bun](https://bun.sh/) — JavaScript 執行環境（可選）

---

## 📄 授權

MIT License

